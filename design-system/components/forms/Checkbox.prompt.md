Brand-green checkbox.

```jsx
<Checkbox label="J'accepte la politique de confidentialité" />
<Checkbox label="Recevoir un rappel par SMS" defaultChecked />
```

Native checkbox attrs (`checked`, `defaultChecked`, `onChange`, `disabled`) pass through.
