Styled native select.

```jsx
<Select label="Service" placeholder="Choisir un service"
  options={['Médecine générale', 'Médecine interne', 'Imagerie médicale', 'Laboratoire']} />
```

Pass `options` as strings or `{value,label}`. Standard select attrs (`value`, `onChange`, `disabled`) pass through.
