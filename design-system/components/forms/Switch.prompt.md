Toggle switch for settings and preferences.

```jsx
<Switch label="Rappels par SMS" defaultChecked />
<Switch label="Bilingue (FR / EN)" />
```

Native checkbox attrs pass through; uses `role="switch"`.
